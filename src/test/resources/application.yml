spring:
  profiles:
    active: test
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=MySQL;DATABASE_TO_LOWER=TRUE;NON_KEYWORDS=USER;
    username: sa
    password:
file:
  upload-dir: ${java.io.tmpdir}/simplecar-api-test

admin:
  username: admin
  password: 123456